--// shhhhnayy05 - Auto Teleport GUI
--// Support: Executor Level 7-8
--// Features: Permanent Save, Delete Confirmation, Anti-Close on Respawn, Auto Reset Script, Window Clone TP

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

--// Config
local CONFIG = {
    GUI_SIZE = UDim2.new(0, 400, 0, 400),
    NAV_GUI_SIZE = UDim2.new(0, 100, 0, 100),
    ICON_SIZE = 50,
    ICON_SPACING = 10,
    SAVE_FILE = "shhhhnayy05_teleport_configs.json",
    COLORS = {
        Background = Color3.fromRGB(20, 20, 25),
        Header = Color3.fromRGB(0, 200, 200),
        AddCheckpoint = Color3.fromRGB(0, 150, 255),
        StartLoop = Color3.fromRGB(0, 200, 100),
        StopLoop = Color3.fromRGB(200, 50, 50),
        SaveConfig = Color3.fromRGB(150, 50, 255),
        LoadConfig = Color3.fromRGB(100, 100, 100),
        Close = Color3.fromRGB(200, 50, 50),
        SpeedBtn = Color3.fromRGB(255, 200, 0),
        SettingsBtn = Color3.fromRGB(100, 100, 150),
        CloneTPBtn = Color3.fromRGB(0, 150, 200),
        Text = Color3.fromRGB(255, 255, 255),
        Notification = Color3.fromRGB(0, 200, 200),
        CheckpointItem = Color3.fromRGB(35, 35, 40),
        TPButton = Color3.fromRGB(0, 150, 50),
        DeleteButton = Color3.fromRGB(150, 30, 30),
        NavBg = Color3.fromRGB(30, 30, 35),
        Warning = Color3.fromRGB(255, 150, 0),
        YesBtn = Color3.fromRGB(200, 50, 50),
        NoBtn = Color3.fromRGB(0, 150, 50)
    }
}

--// Variables - SIMPAN DI GLOBAL AGAR TIDAK HILANG SAAT RESPAWN
if not getgenv().shhhhnayy05_TeleportData then
    getgenv().shhhhnayy05_TeleportData = {
        Checkpoints = {},
        CurrentCheckpointIndex = 1,
        IsLooping = false,
        LoopConnection = nil,
        Speed = 1.0,
        IsMinimized = true,
        SavedConfigs = {},
        CheckpointItems = {},
        GUIInitialized = false,
        
        -- Variables for Auto Reset Loop
        ResetScript = "",
        ResetDelay = 0.5, -- dalam detik (s)
        StartLoopDelay = 1.0, -- delay setelah reset sebelum mulai ke CP1 (s)
        AutoResetEnabled = false,
        IsResetting = false,
        
        -- Fitur Clone
        CloneModel = nil
    }
end

local Data = getgenv().shhhhnayy05_TeleportData

--// File System Functions (PERMANENT SAVE)
local function SaveToFile()
    local success, err = pcall(function()
        local saveData = {
            configs = Data.SavedConfigs,
            lastSaved = os.time()
        }
        local json = HttpService:JSONEncode(saveData)
        writefile(CONFIG.SAVE_FILE, json)
    end)
    if not success then
        warn("shhhhnayy05 - Failed to save: " .. tostring(err))
    end
end

local function LoadFromFile()
    local success, result = pcall(function()
        if isfile(CONFIG.SAVE_FILE) then
            local json = readfile(CONFIG.SAVE_FILE)
            return HttpService:JSONDecode(json)
        end
        return nil
    end)
    
    if success and result then
        Data.SavedConfigs = result.configs or {}
        return true
    else
        Data.SavedConfigs = {}
        return false
    end
end

--// Load data saat startup (HANYA SEKALI)
if not Data.DataLoaded then
    LoadFromFile()
    Data.DataLoaded = true
end

--// Utility Functions
local function CreateTween(instance, properties, duration, easingStyle, easingDirection)
    local tween = TweenService:Create(instance, TweenInfo.new(
        duration or 0.3,
        easingStyle or Enum.EasingStyle.Quad,
        easingDirection or Enum.EasingDirection.Out
    ), properties)
    return tween
end

local function CreateClickEffect(button)
    local originalSize = button.Size
    local originalPosition = button.Position
    
    local shrinkTween = CreateTween(button, {
        Size = UDim2.new(originalSize.X.Scale * 0.95, originalSize.X.Offset * 0.95, 
                         originalSize.Y.Scale * 0.95, originalSize.Y.Offset * 0.95),
        Position = UDim2.new(
            originalPosition.X.Scale, originalPosition.X.Offset + (originalSize.X.Offset * 0.025),
            originalPosition.Y.Scale, originalPosition.Y.Offset + (originalSize.Y.Offset * 0.025)
        )
    }, 0.1)
    
    local expandTween = CreateTween(button, {
        Size = originalSize,
        Position = originalPosition
    }, 0.1)
    
    shrinkTween:Play()
    shrinkTween.Completed:Connect(function()
        expandTween:Play()
    end)
    
    local ripple = Instance.new("Frame")
    ripple.Name = "Ripple"
    ripple.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    ripple.BackgroundTransparency = 0.7
    ripple.BorderSizePixel = 0
    ripple.Size = UDim2.new(0, 0, 0, 0)
    ripple.Position = UDim2.new(0.5, 0, 0.5, 0)
    ripple.AnchorPoint = Vector2.new(0.5, 0.5)
    ripple.ZIndex = button.ZIndex + 1
    ripple.Parent = button
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = ripple
    
    local expandRipple = CreateTween(ripple, {
        Size = UDim2.new(2, 0, 2, 0),
        BackgroundTransparency = 1
    }, 0.5)
    
    expandRipple:Play()
    expandRipple.Completed:Connect(function()
        ripple:Destroy()
    end)
end

local function ShowNotification(text, duration)
    duration = duration or 2
    
    for _, child in ipairs(PlayerGui:GetChildren()) do
        if child.Name == "shhhhnayy05_Notification" then
            child:Destroy()
        end
    end
    
    local notifGui = Instance.new("ScreenGui")
    notifGui.Name = "shhhhnayy05_Notification"
    notifGui.ResetOnSpawn = false
    notifGui.Parent = PlayerGui
    
    local frame = Instance.new("Frame")
    frame.BackgroundColor3 = CONFIG.COLORS.Background
    frame.BackgroundTransparency = 0.2
    frame.BorderSizePixel = 0
    frame.Size = UDim2.new(0, 250, 0, 40)
    frame.Position = UDim2.new(0.5, -125, 0, -50)
    frame.Parent = notifGui
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = frame
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = CONFIG.COLORS.Notification
    stroke.Thickness = 2
    stroke.Parent = frame
    
    local label = Instance.new("TextLabel")
    label.BackgroundTransparency = 1
    label.Size = UDim2.new(1, 0, 1, 0)
    label.Font = Enum.Font.GothamBold
    label.Text = text
    label.TextColor3 = CONFIG.COLORS.Text
    label.TextSize = 14
    label.Parent = frame
    
    local slideIn = CreateTween(frame, {Position = UDim2.new(0.5, -125, 0, 20)}, 0.3, Enum.EasingStyle.Back)
    slideIn:Play()
    
    task.delay(duration, function()
        local slideOut = CreateTween(frame, {Position = UDim2.new(0.5, -125, 0, -50)}, 0.3)
        slideOut:Play()
        slideOut.Completed:Connect(function()
            notifGui:Destroy()
        end)
    end)
end

local function GetHumanoidRootPart()
    local character = LocalPlayer.Character
    if character then
        return character:FindFirstChild("HumanoidRootPart")
    end
    return nil
end

local function SafeTeleport(position)
    local hrp = GetHumanoidRootPart()
    if hrp then
        hrp.CFrame = CFrame.new(position)
        return true
    end
    return false
end

--// Create GUI Function
local function CreateGUI()
    local oldGui = PlayerGui:FindFirstChild("shhhhnayy05_AutoTeleport")
    if oldGui then
        oldGui:Destroy()
    end
    
    --// Create Main GUI
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "shhhhnayy05_AutoTeleport"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.Parent = PlayerGui

    --// Main Panel
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainPanel"
    MainFrame.BackgroundColor3 = CONFIG.COLORS.Background
    MainFrame.BorderSizePixel = 0
    MainFrame.Size = Data.IsMinimized and UDim2.new(0, 0, 0, 0) or CONFIG.GUI_SIZE
    MainFrame.Position = UDim2.new(0.5, -200, 0.5, -200)
    MainFrame.Visible = not Data.IsMinimized
    MainFrame.ClipsDescendants = true
    MainFrame.Parent = ScreenGui

    local mainCorner = Instance.new("UICorner")
    mainCorner.CornerRadius = UDim.new(0, 12)
    mainCorner.Parent = MainFrame

    local mainStroke = Instance.new("UIStroke")
    mainStroke.Color = CONFIG.COLORS.Header
    mainStroke.Thickness = 2
    mainStroke.Parent = MainFrame

    --// Header
    local Header = Instance.new("Frame")
    Header.BackgroundColor3 = CONFIG.COLORS.Header
    Header.BorderSizePixel = 0
    Header.Size = UDim2.new(1, 0, 0, 40)
    Header.Parent = MainFrame

    local headerCorner = Instance.new("UICorner")
    headerCorner.CornerRadius = UDim.new(0, 12)
    headerCorner.Parent = Header

    local bottomCover = Instance.new("Frame")
    bottomCover.BackgroundColor3 = CONFIG.COLORS.Header
    bottomCover.BorderSizePixel = 0
    bottomCover.Size = UDim2.new(1, 0, 0, 10)
    bottomCover.Position = UDim2.new(0, 0, 1, -10)
    bottomCover.Parent = Header

    local Title = Instance.new("TextLabel")
    Title.BackgroundTransparency = 1
    Title.Size = UDim2.new(1, -50, 1, 0)
    Title.Position = UDim2.new(0, 10, 0, 0)
    Title.Font = Enum.Font.GothamBold
    Title.Text = "shhhhnayy05 - AUTO TELEPORT"
    Title.TextColor3 = CONFIG.COLORS.Background
    Title.TextSize = 16
    Title.TextXAlignment = Enum.TextXAlignment.Left
    Title.Parent = Header

    --// Minimize Button
    local MinimizeBtn = Instance.new("TextButton")
    MinimizeBtn.BackgroundColor3 = CONFIG.COLORS.SpeedBtn
    MinimizeBtn.BorderSizePixel = 0
    MinimizeBtn.Size = UDim2.new(0, 35, 0, 30)
    MinimizeBtn.Position = UDim2.new(1, -45, 0, 5)
    MinimizeBtn.Font = Enum.Font.GothamBold
    MinimizeBtn.Text = "−"
    MinimizeBtn.TextColor3 = CONFIG.COLORS.Background
    MinimizeBtn.TextSize = 20
    MinimizeBtn.Parent = Header

    local minBtnCorner = Instance.new("UICorner")
    minBtnCorner.CornerRadius = UDim.new(0, 6)
    minBtnCorner.Parent = MinimizeBtn

    --// Split Layout
    local LeftPanel = Instance.new("Frame")
    LeftPanel.BackgroundTransparency = 1
    LeftPanel.Size = UDim2.new(0.55, 0, 1, -50)
    LeftPanel.Position = UDim2.new(0, 10, 0, 50)
    LeftPanel.Parent = MainFrame

    local RightPanel = Instance.new("Frame")
    RightPanel.BackgroundTransparency = 1
    RightPanel.Size = UDim2.new(0.4, -10, 1, -50)
    RightPanel.Position = UDim2.new(0.55, 10, 0, 50)
    RightPanel.Parent = MainFrame

    --// Checkpoint List
    local CheckpointsScroll = Instance.new("ScrollingFrame")
    CheckpointsScroll.Name = "CheckpointsList"
    CheckpointsScroll.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    CheckpointsScroll.BorderSizePixel = 0
    CheckpointsScroll.Size = UDim2.new(1, 0, 1, -30)
    CheckpointsScroll.ScrollBarThickness = 6
    CheckpointsScroll.ScrollBarImageColor3 = CONFIG.COLORS.Header
    CheckpointsScroll.Parent = LeftPanel

    local scrollCorner = Instance.new("UICorner")
    scrollCorner.CornerRadius = UDim.new(0, 8)
    scrollCorner.Parent = CheckpointsScroll

    local checkpointsLayout = Instance.new("UIListLayout")
    checkpointsLayout.Padding = UDim.new(0, 8)
    checkpointsLayout.SortOrder = Enum.SortOrder.LayoutOrder
    checkpointsLayout.Parent = CheckpointsScroll

    local checkpointsPadding = Instance.new("UIPadding")
    checkpointsPadding.PaddingLeft = UDim.new(0, 8)
    checkpointsPadding.PaddingRight = UDim.new(0, 8)
    checkpointsPadding.PaddingTop = UDim.new(0, 8)
    checkpointsPadding.PaddingBottom = UDim.new(0, 8)
    checkpointsPadding.Parent = CheckpointsScroll

    --// Penanda Jumlah Total CP yang Tersimpan
    local TotalCPLabel = Instance.new("TextLabel")
    TotalCPLabel.BackgroundTransparency = 1
    TotalCPLabel.Size = UDim2.new(1, 0, 0, 25)
    TotalCPLabel.Position = UDim2.new(0, 0, 1, -25)
    TotalCPLabel.Font = Enum.Font.GothamBold
    TotalCPLabel.Text = "Total CP Tersimpan: " .. #Data.Checkpoints
    TotalCPLabel.TextColor3 = CONFIG.COLORS.Text
    TotalCPLabel.TextSize = 12
    TotalCPLabel.Parent = LeftPanel

    --// Right Panel Controls
    local RightLayout = Instance.new("UIListLayout")
    RightLayout.Padding = UDim.new(0, 8)
    RightLayout.SortOrder = Enum.SortOrder.LayoutOrder
    RightLayout.Parent = RightPanel

    local function CreateButton(name, text, color, icon, parent)
        parent = parent or RightPanel
        local btn = Instance.new("TextButton")
        btn.Name = name
        btn.BackgroundColor3 = color
        btn.BorderSizePixel = 0
        btn.Size = UDim2.new(1, 0, 0, 38) -- sedikit dikecilkan agar muat tombol baru
        btn.Font = Enum.Font.GothamBold
        btn.Text = (icon and icon .. " " or "") .. text
        btn.TextColor3 = CONFIG.COLORS.Text
        btn.TextSize = 12
        btn.Parent = parent
        
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 8)
        corner.Parent = btn
        
        btn.MouseButton1Click:Connect(function()
            CreateClickEffect(btn)
        end)
        
        return btn
    end

    local StartLoopBtn = CreateButton("StartLoop", "START LOOP", CONFIG.COLORS.StartLoop, "▶")
    local StopLoopBtn = CreateButton("StopLoop", "STOP LOOP", CONFIG.COLORS.StopLoop, "⏹")
    local SaveConfigBtn = CreateButton("SaveConfig", "SAVE CONFIG", CONFIG.COLORS.SaveConfig, "💾")
    local LoadConfigBtn = CreateButton("LoadConfig", "LOAD CONFIG", CONFIG.COLORS.LoadConfig, "📂")

    --// Speed Control
    local SpeedFrame = Instance.new("Frame")
    SpeedFrame.BackgroundTransparency = 1
    SpeedFrame.Size = UDim2.new(1, 0, 0, 45)
    SpeedFrame.LayoutOrder = 5
    SpeedFrame.Parent = RightPanel

    local SpeedLabel = Instance.new("TextLabel")
    SpeedLabel.BackgroundTransparency = 1
    SpeedLabel.Size = UDim2.new(1, 0, 0, 15)
    SpeedLabel.Font = Enum.Font.GothamBold
    SpeedLabel.Text = "SPEED: " .. Data.Speed .. "s"
    SpeedLabel.TextColor3 = CONFIG.COLORS.Text
    SpeedLabel.TextSize = 12
    SpeedLabel.Parent = SpeedFrame

    local MinusBtn = Instance.new("TextButton")
    MinusBtn.BackgroundColor3 = CONFIG.COLORS.SpeedBtn
    MinusBtn.BorderSizePixel = 0
    MinusBtn.Size = UDim2.new(0, 40, 0, 25)
    MinusBtn.Position = UDim2.new(0.5, -45, 0, 18)
    MinusBtn.Font = Enum.Font.GothamBold
    MinusBtn.Text = "-"
    MinusBtn.TextColor3 = CONFIG.COLORS.Background
    MinusBtn.TextSize = 18
    MinusBtn.Parent = SpeedFrame

    local minusCorner = Instance.new("UICorner")
    minusCorner.CornerRadius = UDim.new(0, 6)
    minusCorner.Parent = MinusBtn

    local PlusBtn = Instance.new("TextButton")
    PlusBtn.BackgroundColor3 = CONFIG.COLORS.SpeedBtn
    PlusBtn.BorderSizePixel = 0
    PlusBtn.Size = UDim2.new(0, 40, 0, 25)
    PlusBtn.Position = UDim2.new(0.5, 5, 0, 18)
    PlusBtn.Font = Enum.Font.GothamBold
    PlusBtn.Text = "+"
    PlusBtn.TextColor3 = CONFIG.COLORS.Background
    PlusBtn.TextSize = 18
    PlusBtn.Parent = SpeedFrame

    local plusCorner = Instance.new("UICorner")
    plusCorner.CornerRadius = UDim.new(0, 6)
    plusCorner.Parent = PlusBtn

    --// Tombol Settings
    local SettingsBtn = CreateButton("SettingsBtn", "SETTINGS", CONFIG.COLORS.SettingsBtn, "⚙")
    SettingsBtn.LayoutOrder = 6
    SettingsBtn.Parent = RightPanel

    --// FITUR BARU: Tombol Menu Clone TP di bawah Settings
    local CloneTPBtn = CreateButton("CloneTPBtn", "CLONE TP", CONFIG.COLORS.CloneTPBtn, "👥")
    CloneTPBtn.LayoutOrder = 7
    CloneTPBtn.Parent = RightPanel

    --// ICONS
    local CheckpointIcon = Instance.new("TextButton")
    CheckpointIcon.Name = "CheckpointIcon"
    CheckpointIcon.BackgroundColor3 = CONFIG.COLORS.AddCheckpoint
    CheckpointIcon.BorderSizePixel = 0
    CheckpointIcon.Size = UDim2.new(0, CONFIG.ICON_SIZE, 0, CONFIG.ICON_SIZE)
    CheckpointIcon.Position = UDim2.new(1, -60, 0, 10)
    CheckpointIcon.Font = Enum.Font.GothamBold
    CheckpointIcon.Text = "📍"
    CheckpointIcon.TextColor3 = CONFIG.COLORS.Text
    CheckpointIcon.TextSize = 24
    CheckpointIcon.AutoButtonColor = false
    CheckpointIcon.Active = false
    CheckpointIcon.Parent = ScreenGui

    local checkCorner = Instance.new("UICorner")
    checkCorner.CornerRadius = UDim.new(0, 10)
    checkCorner.Parent = CheckpointIcon

    local checkStroke = Instance.new("UIStroke")
    checkStroke.Color = Color3.fromRGB(255, 255, 255)
    checkStroke.Thickness = 2
    checkStroke.Parent = CheckpointIcon

    local NavIcon = Instance.new("TextButton")
    NavIcon.Name = "NavIcon"
    NavIcon.BackgroundColor3 = CONFIG.COLORS.Header
    NavIcon.BorderSizePixel = 0
    NavIcon.Size = UDim2.new(0, CONFIG.ICON_SIZE, 0, CONFIG.ICON_SIZE)
    NavIcon.Position = UDim2.new(1, -60, 0, 10 + CONFIG.ICON_SIZE + CONFIG.ICON_SPACING)
    NavIcon.Font = Enum.Font.GothamBold
    NavIcon.Text = "🧭"
    NavIcon.TextColor3 = CONFIG.COLORS.Background
    NavIcon.TextSize = 24
    NavIcon.AutoButtonColor = false
    NavIcon.Active = false
    NavIcon.Parent = ScreenGui

    local navCorner = Instance.new("UICorner")
    navCorner.CornerRadius = UDim.new(0, 10)
    navCorner.Parent = NavIcon

    local navStroke = Instance.new("UIStroke")
    navStroke.Color = Color3.fromRGB(255, 255, 255)
    navStroke.Thickness = 2
    navStroke.Parent = NavIcon

    local MinimizeIcon = Instance.new("TextButton")
    MinimizeIcon.Name = "MinimizeIcon"
    MinimizeIcon.BackgroundColor3 = CONFIG.COLORS.Header
    MinimizeIcon.BorderSizePixel = 0
    MinimizeIcon.Size = UDim2.new(0, CONFIG.ICON_SIZE, 0, CONFIG.ICON_SIZE)
    MinimizeIcon.Position = UDim2.new(0, 10, 0, 10)
    MinimizeIcon.Font = Enum.Font.GothamBold
    MinimizeIcon.Text = "🎯"
    MinimizeIcon.TextColor3 = CONFIG.COLORS.Background
    MinimizeIcon.TextSize = 24
    MinimizeIcon.Visible = Data.IsMinimized
    MinimizeIcon.AutoButtonColor = false
    MinimizeIcon.Active = false
    MinimizeIcon.Parent = ScreenGui

    local minIconCorner = Instance.new("UICorner")
    minIconCorner.CornerRadius = UDim.new(0, 10)
    minIconCorner.Parent = MinimizeIcon

    local minIconStroke = Instance.new("UIStroke")
    minIconStroke.Color = Color3.fromRGB(255, 255, 255)
    minIconStroke.Thickness = 2
    minIconStroke.Parent = MinimizeIcon

    --// Navigation GUI
    local NavGui = Instance.new("Frame")
    NavGui.Name = "NavGui"
    NavGui.BackgroundColor3 = CONFIG.COLORS.NavBg
    NavGui.BorderSizePixel = 0
    NavGui.Size = CONFIG.NAV_GUI_SIZE
    NavGui.Position = UDim2.new(1, -170, 0, 10 + CONFIG.ICON_SIZE + CONFIG.ICON_SPACING)
    NavGui.Visible = false
    NavGui.Parent = ScreenGui

    local navGuiCorner = Instance.new("UICorner")
    navGuiCorner.CornerRadius = UDim.new(0, 12)
    navGuiCorner.Parent = NavGui

    local navGuiStroke = Instance.new("UIStroke")
    navGuiStroke.Color = CONFIG.COLORS.Header
    navGuiStroke.Thickness = 2
    navGuiStroke.Parent = NavGui

    local NavHeader = Instance.new("Frame")
    NavHeader.BackgroundColor3 = CONFIG.COLORS.Header
    NavHeader.BorderSizePixel = 0
    NavHeader.Size = UDim2.new(1, 0, 0, 25)
    NavHeader.Parent = NavGui

    local navHeaderCorner = Instance.new("UICorner")
    navHeaderCorner.CornerRadius = UDim.new(0, 12)
    navHeaderCorner.Parent = NavHeader

    local navHeaderCover = Instance.new("Frame")
    navHeaderCover.BackgroundColor3 = CONFIG.COLORS.Header
    navHeaderCover.BorderSizePixel = 0
    navHeaderCover.Size = UDim2.new(1, 0, 0, 10)
    navHeaderCover.Position = UDim2.new(0, 0, 1, -10)
    navHeaderCover.Parent = NavHeader

    local NavTitle = Instance.new("TextLabel")
    NavTitle.BackgroundTransparency = 1
    NavTitle.Size = UDim2.new(1, -30, 1, 0)
    NavTitle.Position = UDim2.new(0, 5, 0, 0)
    NavTitle.Font = Enum.Font.GothamBold
    NavTitle.Text = "NAV"
    NavTitle.TextColor3 = CONFIG.COLORS.Background
    NavTitle.TextSize = 12
    NavTitle.Parent = NavHeader

    local CloseNavBtn = Instance.new("TextButton")
    CloseNavBtn.BackgroundColor3 = CONFIG.COLORS.Close
    CloseNavBtn.BorderSizePixel = 0
    CloseNavBtn.Size = UDim2.new(0, 20, 0, 20)
    CloseNavBtn.Position = UDim2.new(1, -25, 0, 2)
    CloseNavBtn.Font = Enum.Font.GothamBold
    CloseNavBtn.Text = "X"
    CloseNavBtn.TextColor3 = CONFIG.COLORS.Text
    CloseNavBtn.TextSize = 12
    CloseNavBtn.Parent = NavHeader

    local closeNavCorner = Instance.new("UICorner")
    closeNavCorner.CornerRadius = UDim.new(0, 6)
    closeNavCorner.Parent = CloseNavBtn

    local NavButtonsContainer = Instance.new("Frame")
    NavButtonsContainer.BackgroundTransparency = 1
    NavButtonsContainer.Size = UDim2.new(1, 0, 1, -35)
    NavButtonsContainer.Position = UDim2.new(0, 0, 0, 30)
    NavButtonsContainer.Parent = NavGui

    local PrevBtn = Instance.new("TextButton")
    PrevBtn.Name = "PrevBtn"
    PrevBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
    PrevBtn.BorderSizePixel = 0
    PrevBtn.Size = UDim2.new(0, 40, 0, 40)
    PrevBtn.Position = UDim2.new(0, 8, 0.5, -20)
    PrevBtn.Font = Enum.Font.GothamBold
    PrevBtn.Text = "⬅️"
    PrevBtn.TextColor3 = CONFIG.COLORS.Text
    PrevBtn.TextSize = 20
    PrevBtn.Parent = NavButtonsContainer

    local prevCorner = Instance.new("UICorner")
    prevCorner.CornerRadius = UDim.new(0, 8)
    prevCorner.Parent = PrevBtn

    local NextBtn = Instance.new("TextButton")
    NextBtn.Name = "NextBtn"
    NextBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
    NextBtn.BorderSizePixel = 0
    NextBtn.Size = UDim2.new(0, 40, 0, 40)
    NextBtn.Position = UDim2.new(1, -48, 0.5, -20)
    NextBtn.Font = Enum.Font.GothamBold
    NextBtn.Text = "➡️"
    NextBtn.TextColor3 = CONFIG.COLORS.Text
    NextBtn.TextSize = 20
    NextBtn.Parent = NavButtonsContainer

    local nextCorner = Instance.new("UICorner")
    nextCorner.CornerRadius = UDim.new(0, 8)
    nextCorner.Parent = NextBtn

    local CurrentIndexLabel = Instance.new("TextLabel")
    CurrentIndexLabel.BackgroundTransparency = 1
    CurrentIndexLabel.Size = UDim2.new(1, 0, 0, 20)
    CurrentIndexLabel.Position = UDim2.new(0, 0, 1, -5)
    CurrentIndexLabel.Font = Enum.Font.GothamBold
    CurrentIndexLabel.Text = Data.CurrentCheckpointIndex .. " / " .. #Data.Checkpoints
    CurrentIndexLabel.TextColor3 = CONFIG.COLORS.Text
    CurrentIndexLabel.TextSize = 11
    CurrentIndexLabel.Parent = NavButtonsContainer

    --// Load Config Window
    local LoadConfigFrame = Instance.new("Frame")
    LoadConfigFrame.Name = "LoadConfigWindow"
    LoadConfigFrame.BackgroundColor3 = CONFIG.COLORS.Background
    LoadConfigFrame.BorderSizePixel = 0
    LoadConfigFrame.Size = UDim2.new(0, 300, 0, 300)
    LoadConfigFrame.Position = UDim2.new(0.5, -150, 0.5, -150)
    LoadConfigFrame.Visible = false
    LoadConfigFrame.Parent = ScreenGui

    local loadCorner = Instance.new("UICorner")
    loadCorner.CornerRadius = UDim.new(0, 12)
    loadCorner.Parent = LoadConfigFrame

    local loadStroke = Instance.new("UIStroke")
    loadStroke.Color = CONFIG.COLORS.Header
    loadStroke.Thickness = 2
    loadStroke.Parent = LoadConfigFrame

    local LoadHeader = Instance.new("Frame")
    LoadHeader.BackgroundColor3 = CONFIG.COLORS.Header
    LoadHeader.BorderSizePixel = 0
    LoadHeader.Size = UDim2.new(1, 0, 0, 40)
    LoadHeader.Parent = LoadConfigFrame

    local loadHeaderCorner = Instance.new("UICorner")
    loadHeaderCorner.CornerRadius = UDim.new(0, 12)
    loadHeaderCorner.Parent = LoadHeader

    local loadBottomCover = Instance.new("Frame")
    loadBottomCover.BackgroundColor3 = CONFIG.COLORS.Header
    loadBottomCover.BorderSizePixel = 0
    loadBottomCover.Size = UDim2.new(1, 0, 0, 10)
    loadBottomCover.Position = UDim2.new(0, 0, 1, -10)
    loadBottomCover.Parent = LoadHeader

    local LoadTitle = Instance.new("TextLabel")
    LoadTitle.BackgroundTransparency = 1
    LoadTitle.Size = UDim2.new(1, -50, 1, 0)
    LoadTitle.Position = UDim2.new(0, 10, 0, 0)
    LoadTitle.Font = Enum.Font.GothamBold
    LoadTitle.Text = "LOAD CONFIG"
    LoadTitle.TextColor3 = CONFIG.COLORS.Background
    LoadTitle.TextSize = 16
    LoadTitle.TextXAlignment = Enum.TextXAlignment.Left
    LoadTitle.Parent = LoadHeader

    local CloseLoadBtn = Instance.new("TextButton")
    CloseLoadBtn.BackgroundColor3 = CONFIG.COLORS.Close
    CloseLoadBtn.BorderSizePixel = 0
    CloseLoadBtn.Size = UDim2.new(0, 35, 0, 30)
    CloseLoadBtn.Position = UDim2.new(1, -40, 0, 5)
    CloseLoadBtn.Font = Enum.Font.GothamBold
    CloseLoadBtn.Text = "X"
    CloseLoadBtn.TextColor3 = CONFIG.COLORS.Text
    CloseLoadBtn.TextSize = 14
    CloseLoadBtn.Parent = LoadHeader

    local closeLoadCorner = Instance.new("UICorner")
    closeLoadCorner.CornerRadius = UDim.new(0, 6)
    closeLoadCorner.Parent = CloseLoadBtn

    local ScrollFrame = Instance.new("ScrollingFrame")
    ScrollFrame.BackgroundTransparency = 1
    ScrollFrame.Size = UDim2.new(1, -20, 1, -60)
    ScrollFrame.Position = UDim2.new(0, 10, 0, 50)
    ScrollFrame.ScrollBarThickness = 6
    ScrollFrame.ScrollBarImageColor3 = CONFIG.COLORS.Header
    ScrollFrame.Parent = LoadConfigFrame

    local scrollLayout = Instance.new("UIListLayout")
    scrollLayout.Padding = UDim.new(0, 8)
    scrollLayout.SortOrder = Enum.SortOrder.LayoutOrder
    scrollLayout.Parent = ScrollFrame

    --// Settings Window
    local SettingsWindow = Instance.new("Frame")
    SettingsWindow.Name = "SettingsWindow"
    SettingsWindow.BackgroundColor3 = CONFIG.COLORS.Background
    SettingsWindow.BorderSizePixel = 0
    SettingsWindow.Size = UDim2.new(0, 320, 0, 310)
    SettingsWindow.Position = UDim2.new(0.5, -160, 0.5, -155)
    SettingsWindow.Visible = false
    SettingsWindow.Parent = ScreenGui

    local settingsWinCorner = Instance.new("UICorner")
    settingsWinCorner.CornerRadius = UDim.new(0, 12)
    settingsWinCorner.Parent = SettingsWindow

    local settingsWinStroke = Instance.new("UIStroke")
    settingsWinStroke.Color = CONFIG.COLORS.Header
    settingsWinStroke.Thickness = 2
    settingsWinStroke.Parent = SettingsWindow

    local SettingsHeader = Instance.new("Frame")
    SettingsHeader.BackgroundColor3 = CONFIG.COLORS.Header
    SettingsHeader.BorderSizePixel = 0
    SettingsHeader.Size = UDim2.new(1, 0, 0, 40)
    SettingsHeader.Parent = SettingsWindow

    local settingsHeaderCorner = Instance.new("UICorner")
    settingsHeaderCorner.CornerRadius = UDim.new(0, 12)
    settingsHeaderCorner.Parent = SettingsHeader

    local settingsHeaderCover = Instance.new("Frame")
    settingsHeaderCover.BackgroundColor3 = CONFIG.COLORS.Header
    settingsHeaderCover.BorderSizePixel = 0
    settingsHeaderCover.Size = UDim2.new(1, 0, 0, 10)
    settingsHeaderCover.Position = UDim2.new(0, 0, 1, -10)
    settingsHeaderCover.Parent = SettingsHeader

    local SettingsTitleLabel = Instance.new("TextLabel")
    SettingsTitleLabel.BackgroundTransparency = 1
    SettingsTitleLabel.Size = UDim2.new(1, -50, 1, 0)
    SettingsTitleLabel.Position = UDim2.new(0, 10, 0, 0)
    SettingsTitleLabel.Font = Enum.Font.GothamBold
    SettingsTitleLabel.Text = "TELEPORT SETTINGS"
    SettingsTitleLabel.TextColor3 = CONFIG.COLORS.Background
    SettingsTitleLabel.TextSize = 16
    SettingsTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    SettingsTitleLabel.Parent = SettingsHeader

    local CloseSettingsBtn = Instance.new("TextButton")
    CloseSettingsBtn.BackgroundColor3 = CONFIG.COLORS.Close
    CloseSettingsBtn.BorderSizePixel = 0
    CloseSettingsBtn.Size = UDim2.new(0, 35, 0, 30)
    CloseSettingsBtn.Position = UDim2.new(1, -40, 0, 5)
    CloseSettingsBtn.Font = Enum.Font.GothamBold
    CloseSettingsBtn.Text = "X"
    CloseSettingsBtn.TextColor3 = CONFIG.COLORS.Text
    CloseSettingsBtn.TextSize = 14
    CloseSettingsBtn.Parent = SettingsHeader

    local closeSettingsCorner = Instance.new("UICorner")
    closeSettingsCorner.CornerRadius = UDim.new(0, 6)
    closeSettingsCorner.Parent = CloseSettingsBtn

    -- Script Input UI
    local ScriptTitle = Instance.new("TextLabel")
    ScriptTitle.BackgroundTransparency = 1
    ScriptTitle.Size = UDim2.new(1, -20, 0, 20)
    ScriptTitle.Position = UDim2.new(0, 10, 0, 50)
    ScriptTitle.Font = Enum.Font.GothamBold
    ScriptTitle.Text = "Paste Remote Script (Misal: Reset CP):"
    ScriptTitle.TextColor3 = CONFIG.COLORS.Text
    ScriptTitle.TextSize = 12
    ScriptTitle.TextXAlignment = Enum.TextXAlignment.Left
    ScriptTitle.Parent = SettingsWindow

    local ScriptBox = Instance.new("TextBox")
    ScriptBox.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    ScriptBox.BorderSizePixel = 0
    ScriptBox.Size = UDim2.new(1, -20, 0, 60)
    ScriptBox.Position = UDim2.new(0, 10, 0, 70)
    ScriptBox.Font = Enum.Font.Gotham
    ScriptBox.Text = Data.ResetScript
    ScriptBox.PlaceholderText = "game.ReplicatedStorage..."
    ScriptBox.TextColor3 = CONFIG.COLORS.Text
    ScriptBox.TextSize = 12
    ScriptBox.TextWrapped = true
    ScriptBox.ClearTextOnFocus = false
    ScriptBox.TextXAlignment = Enum.TextXAlignment.Left
    ScriptBox.TextYAlignment = Enum.TextYAlignment.Top
    ScriptBox.Parent = SettingsWindow

    local scriptBoxCorner = Instance.new("UICorner")
    scriptBoxCorner.CornerRadius = UDim.new(0, 8)
    scriptBoxCorner.Parent = ScriptBox

    ScriptBox.FocusLost:Connect(function()
        Data.ResetScript = ScriptBox.Text
    end)

    -- Delay Reset Script UI
    local DelayTitle = Instance.new("TextLabel")
    DelayTitle.BackgroundTransparency = 1
    DelayTitle.Size = UDim2.new(0.6, 0, 0, 30)
    DelayTitle.Position = UDim2.new(0, 10, 0, 140)
    DelayTitle.Font = Enum.Font.GothamBold
    DelayTitle.Text = "Delay Reset Script (s):"
    DelayTitle.TextColor3 = CONFIG.COLORS.Text
    DelayTitle.TextSize = 12
    DelayTitle.TextXAlignment = Enum.TextXAlignment.Left
    DelayTitle.Parent = SettingsWindow

    local DelayBox = Instance.new("TextBox")
    DelayBox.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    DelayBox.BorderSizePixel = 0
    DelayBox.Size = UDim2.new(0.35, 0, 0, 30)
    DelayBox.Position = UDim2.new(0.6, 0, 0, 140)
    DelayBox.Font = Enum.Font.GothamBold
    DelayBox.Text = tostring(Data.ResetDelay)
    DelayBox.TextColor3 = CONFIG.COLORS.Text
    DelayBox.TextSize = 14
    DelayBox.ClearTextOnFocus = false
    DelayBox.Parent = SettingsWindow

    local delayBoxCorner = Instance.new("UICorner")
    delayBoxCorner.CornerRadius = UDim.new(0, 8)
    delayBoxCorner.Parent = DelayBox

    DelayBox.FocusLost:Connect(function()
        local num = tonumber(DelayBox.Text)
        if num then
            Data.ResetDelay = num
            DelayBox.Text = tostring(Data.ResetDelay)
        else
            DelayBox.Text = tostring(Data.ResetDelay)
        end
    end)

    -- Delay Mulai CP setelah Reset
    local StartDelayTitle = Instance.new("TextLabel")
    StartDelayTitle.BackgroundTransparency = 1
    StartDelayTitle.Size = UDim2.new(0.6, 0, 0, 30)
    StartDelayTitle.Position = UDim2.new(0, 10, 0, 180)
    StartDelayTitle.Font = Enum.Font.GothamBold
    StartDelayTitle.Text = "Delay Mulai Ulang (s):"
    StartDelayTitle.TextColor3 = CONFIG.COLORS.Text
    StartDelayTitle.TextSize = 12
    StartDelayTitle.TextXAlignment = Enum.TextXAlignment.Left
    StartDelayTitle.Parent = SettingsWindow

    local StartDelayBox = Instance.new("TextBox")
    StartDelayBox.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    StartDelayBox.BorderSizePixel = 0
    StartDelayBox.Size = UDim2.new(0.35, 0, 0, 30)
    StartDelayBox.Position = UDim2.new(0.6, 0, 0, 180)
    StartDelayBox.Font = Enum.Font.GothamBold
    StartDelayBox.Text = tostring(Data.StartLoopDelay)
    StartDelayBox.TextColor3 = CONFIG.COLORS.Text
    StartDelayBox.TextSize = 14
    StartDelayBox.ClearTextOnFocus = false
    StartDelayBox.Parent = SettingsWindow

    local startDelayBoxCorner = Instance.new("UICorner")
    startDelayBoxCorner.CornerRadius = UDim.new(0, 8)
    startDelayBoxCorner.Parent = StartDelayBox

    StartDelayBox.FocusLost:Connect(function()
        local num = tonumber(StartDelayBox.Text)
        if num then
            Data.StartLoopDelay = num
            StartDelayBox.Text = tostring(Data.StartLoopDelay)
        else
            StartDelayBox.Text = tostring(Data.StartLoopDelay)
        end
    end)

    -- Toggle Reset Loop UI
    local ToggleLoopBtn = Instance.new("TextButton")
    ToggleLoopBtn.BackgroundColor3 = Data.AutoResetEnabled and CONFIG.COLORS.StartLoop or CONFIG.COLORS.StopLoop
    ToggleLoopBtn.BorderSizePixel = 0
    ToggleLoopBtn.Size = UDim2.new(1, -20, 0, 40)
    ToggleLoopBtn.Position = UDim2.new(0, 10, 0, 230)
    ToggleLoopBtn.Font = Enum.Font.GothamBold
    ToggleLoopBtn.Text = "AUTO RESET LOOP: " .. (Data.AutoResetEnabled and "ON" or "OFF")
    ToggleLoopBtn.TextColor3 = CONFIG.COLORS.Text
    ToggleLoopBtn.TextSize = 14
    ToggleLoopBtn.Parent = SettingsWindow

    local toggleLoopCorner = Instance.new("UICorner")
    toggleLoopCorner.CornerRadius = UDim.new(0, 8)
    toggleLoopCorner.Parent = ToggleLoopBtn

    ToggleLoopBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(ToggleLoopBtn)
        Data.AutoResetEnabled = not Data.AutoResetEnabled
        ToggleLoopBtn.BackgroundColor3 = Data.AutoResetEnabled and CONFIG.COLORS.StartLoop or CONFIG.COLORS.StopLoop
        ToggleLoopBtn.Text = "AUTO RESET LOOP: " .. (Data.AutoResetEnabled and "ON" or "OFF")
    end)

    --// FITUR BARU: JENDELA KHUSUS CLONE TP
    local CloneWindow = Instance.new("Frame")
    CloneWindow.Name = "CloneWindow"
    CloneWindow.BackgroundColor3 = CONFIG.COLORS.Background
    CloneWindow.BorderSizePixel = 0
    CloneWindow.Size = UDim2.new(0, 300, 0, 180)
    CloneWindow.Position = UDim2.new(0.5, -150, 0.5, -90)
    CloneWindow.Visible = false
    CloneWindow.Parent = ScreenGui

    local cloneWinCorner = Instance.new("UICorner")
    cloneWinCorner.CornerRadius = UDim.new(0, 12)
    cloneWinCorner.Parent = CloneWindow

    local cloneWinStroke = Instance.new("UIStroke")
    cloneWinStroke.Color = CONFIG.COLORS.CloneTPBtn
    cloneWinStroke.Thickness = 2
    cloneWinStroke.Parent = CloneWindow

    local CloneHeader = Instance.new("Frame")
    CloneHeader.BackgroundColor3 = CONFIG.COLORS.CloneTPBtn
    CloneHeader.BorderSizePixel = 0
    CloneHeader.Size = UDim2.new(1, 0, 0, 40)
    CloneHeader.Parent = CloneWindow

    local cloneHeaderCorner = Instance.new("UICorner")
    cloneHeaderCorner.CornerRadius = UDim.new(0, 12)
    cloneHeaderCorner.Parent = CloneHeader

    local cloneHeaderCover = Instance.new("Frame")
    cloneHeaderCover.BackgroundColor3 = CONFIG.COLORS.CloneTPBtn
    cloneHeaderCover.BorderSizePixel = 0
    cloneHeaderCover.Size = UDim2.new(1, 0, 0, 10)
    cloneHeaderCover.Position = UDim2.new(0, 0, 1, -10)
    cloneHeaderCover.Parent = CloneHeader

    local CloneTitleLabel = Instance.new("TextLabel")
    CloneTitleLabel.BackgroundTransparency = 1
    CloneTitleLabel.Size = UDim2.new(1, -50, 1, 0)
    CloneTitleLabel.Position = UDim2.new(0, 10, 0, 0)
    CloneTitleLabel.Font = Enum.Font.GothamBold
    CloneTitleLabel.Text = "CLONE TP SYSTEM"
    CloneTitleLabel.TextColor3 = CONFIG.COLORS.Background
    CloneTitleLabel.TextSize = 16
    CloneTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    CloneTitleLabel.Parent = CloneHeader

    local CloseCloneBtn = Instance.new("TextButton")
    CloseCloneBtn.BackgroundColor3 = CONFIG.COLORS.Close
    CloseCloneBtn.BorderSizePixel = 0
    CloseCloneBtn.Size = UDim2.new(0, 35, 0, 30)
    CloseCloneBtn.Position = UDim2.new(1, -40, 0, 5)
    CloseCloneBtn.Font = Enum.Font.GothamBold
    CloseCloneBtn.Text = "X"
    CloseCloneBtn.TextColor3 = CONFIG.COLORS.Text
    CloseCloneBtn.TextSize = 14
    CloseCloneBtn.Parent = CloneHeader

    local closeCloneCorner = Instance.new("UICorner")
    closeCloneCorner.CornerRadius = UDim.new(0, 6)
    closeCloneCorner.Parent = CloseCloneBtn

    -- Tombol Add Clone
    local AddCloneBtn = Instance.new("TextButton")
    AddCloneBtn.BackgroundColor3 = Data.CloneModel and CONFIG.COLORS.StopLoop or CONFIG.COLORS.StartLoop
    AddCloneBtn.BorderSizePixel = 0
    AddCloneBtn.Size = UDim2.new(1, -40, 0, 45)
    AddCloneBtn.Position = UDim2.new(0, 20, 0, 60)
    AddCloneBtn.Font = Enum.Font.GothamBold
    AddCloneBtn.Text = Data.CloneModel and "REMOVE CLONE" or "ADD CLONE"
    AddCloneBtn.TextColor3 = CONFIG.COLORS.Text
    AddCloneBtn.TextSize = 14
    AddCloneBtn.Parent = CloneWindow

    local addCloneCorner = Instance.new("UICorner")
    addCloneCorner.CornerRadius = UDim.new(0, 8)
    addCloneCorner.Parent = AddCloneBtn

    -- Tombol Loop Clone
    local LoopCloneBtn = Instance.new("TextButton")
    LoopCloneBtn.BackgroundColor3 = CONFIG.COLORS.AddCheckpoint
    LoopCloneBtn.BorderSizePixel = 0
    LoopCloneBtn.Size = UDim2.new(1, -40, 0, 45)
    LoopCloneBtn.Position = UDim2.new(0, 20, 0, 115)
    LoopCloneBtn.Font = Enum.Font.GothamBold
    LoopCloneBtn.Text = "LOOP CLONE"
    LoopCloneBtn.TextColor3 = CONFIG.COLORS.Text
    LoopCloneBtn.TextSize = 14
    LoopCloneBtn.Parent = CloneWindow

    local loopCloneCorner = Instance.new("UICorner")
    loopCloneCorner.CornerRadius = UDim.new(0, 8)
    loopCloneCorner.Parent = LoopCloneBtn


    --// Delete Confirmation GUI
    local DeleteConfirmGui = Instance.new("Frame")
    DeleteConfirmGui.Name = "DeleteConfirmGui"
    DeleteConfirmGui.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    DeleteConfirmGui.BackgroundTransparency = 0.5
    DeleteConfirmGui.BorderSizePixel = 0
    DeleteConfirmGui.Size = UDim2.new(1, 0, 1, 0)
    DeleteConfirmGui.Visible = false
    DeleteConfirmGui.ZIndex = 100
    DeleteConfirmGui.Parent = ScreenGui

    local ConfirmFrame = Instance.new("Frame")
    ConfirmFrame.BackgroundColor3 = CONFIG.COLORS.Background
    ConfirmFrame.BorderSizePixel = 0
    ConfirmFrame.Size = UDim2.new(0, 280, 0, 120)
    ConfirmFrame.Position = UDim2.new(0.5, -140, 0.5, -60)
    ConfirmFrame.ZIndex = 101
    ConfirmFrame.Parent = DeleteConfirmGui

    local confirmCorner = Instance.new("UICorner")
    confirmCorner.CornerRadius = UDim.new(0, 12)
    confirmCorner.Parent = ConfirmFrame

    local confirmStroke = Instance.new("UIStroke")
    confirmStroke.Color = CONFIG.COLORS.Warning
    confirmStroke.Thickness = 2
    confirmStroke.Parent = ConfirmFrame

    local WarningIcon = Instance.new("TextLabel")
    WarningIcon.BackgroundTransparency = 1
    WarningIcon.Size = UDim2.new(0, 40, 0, 40)
    WarningIcon.Position = UDim2.new(0.5, -20, 0, 5)
    WarningIcon.Font = Enum.Font.GothamBold
    WarningIcon.Text = "⚠️"
    WarningIcon.TextColor3 = CONFIG.COLORS.Warning
    WarningIcon.TextSize = 30
    WarningIcon.ZIndex = 102
    WarningIcon.Parent = ConfirmFrame

    local WarningText = Instance.new("TextLabel")
    WarningText.BackgroundTransparency = 1
    WarningText.Size = UDim2.new(1, -20, 0, 40)
    WarningText.Position = UDim2.new(0, 10, 0, 30)
    WarningText.Font = Enum.Font.GothamBold
    WarningText.Text = "Apakah anda yakin\nmenghapus file ini?"
    WarningText.TextColor3 = CONFIG.COLORS.Text
    WarningText.TextSize = 14
    WarningText.TextWrapped = true
    WarningText.ZIndex = 102
    WarningText.Parent = ConfirmFrame

    local YesBtn = Instance.new("TextButton")
    YesBtn.Name = "YesBtn"
    YesBtn.BackgroundColor3 = CONFIG.COLORS.YesBtn
    YesBtn.BorderSizePixel = 0
    YesBtn.Size = UDim2.new(0, 100, 0, 35)
    YesBtn.Position = UDim2.new(0.25, -50, 1, -45)
    YesBtn.Font = Enum.Font.GothamBold
    YesBtn.Text = "YES"
    YesBtn.TextColor3 = CONFIG.COLORS.Text
    YesBtn.TextSize = 14
    YesBtn.ZIndex = 102
    YesBtn.Parent = ConfirmFrame

    local yesCorner = Instance.new("UICorner")
    yesCorner.CornerRadius = UDim.new(0, 8)
    yesCorner.Parent = YesBtn

    local NoBtn = Instance.new("TextButton")
    NoBtn.Name = "NoBtn"
    NoBtn.BackgroundColor3 = CONFIG.COLORS.NoBtn
    NoBtn.BorderSizePixel = 0
    NoBtn.Size = UDim2.new(0, 100, 0, 35)
    NoBtn.Position = UDim2.new(0.75, -50, 1, -45)
    NoBtn.Font = Enum.Font.GothamBold
    NoBtn.Text = "NO"
    NoBtn.TextColor3 = CONFIG.COLORS.Text
    NoBtn.TextSize = 14
    NoBtn.ZIndex = 102
    NoBtn.Parent = ConfirmFrame

    local noCorner = Instance.new("UICorner")
    noCorner.CornerRadius = UDim.new(0, 8)
    noCorner.Parent = NoBtn

    --// Functions
    local function UpdateCurrentIndexLabel()
        CurrentIndexLabel.Text = Data.CurrentCheckpointIndex .. " / " .. #Data.Checkpoints
        if TotalCPLabel then
            TotalCPLabel.Text = "Total CP Tersimpan: " .. #Data.Checkpoints
        end
    end

    local function HandleManualAutoReset()
        if Data.AutoResetEnabled and Data.CurrentCheckpointIndex == #Data.Checkpoints and not Data.IsLooping then
            task.spawn(function()
                if Data.IsResetting then return end
                Data.IsResetting = true
                
                task.wait(Data.ResetDelay or 0.5)
                
                if Data.ResetScript and Data.ResetScript ~= "" then
                    local success, result = pcall(function()
                        local func = loadstring(Data.ResetScript)
                        if func then func() end
                    end)
                    if not success then
                        warn("shhhhnayy05 - Reset Script Error: ", result)
                    end
                end
                
                task.wait(Data.StartLoopDelay or 1.0)
                
                if Data.Checkpoints[1] then
                    SafeTeleport(Data.Checkpoints[1].Position)
                    Data.CurrentCheckpointIndex = 1
                    UpdateCurrentIndexLabel()
                    ShowNotification("Auto Reset Terpicu!")
                end
                
                Data.IsResetting = false
            end)
        end
    end

    local function TeleportToCheckpoint(index)
        if index < 1 or index > #Data.Checkpoints then
            ShowNotification("Invalid checkpoint!")
            return
        end
        
        if SafeTeleport(Data.Checkpoints[index].Position) then
            Data.CurrentCheckpointIndex = index
            UpdateCurrentIndexLabel()
            ShowNotification("Checkpoint " .. index)
            
            HandleManualAutoReset()
        else
            ShowNotification("Character not ready!")
        end
    end

    local function CreateCheckpointItem(index, position)
        local item = Instance.new("Frame")
        item.Name = "Checkpoint_" .. index
        item.BackgroundColor3 = CONFIG.COLORS.CheckpointItem
        item.BorderSizePixel = 0
        item.Size = UDim2.new(1, 0, 0, 45)
        item.LayoutOrder = index
        item.Parent = CheckpointsScroll
        
        local itemCorner = Instance.new("UICorner")
        itemCorner.CornerRadius = UDim.new(0, 8)
        itemCorner.Parent = item
        
        local nameLabel = Instance.new("TextLabel")
        nameLabel.BackgroundTransparency = 1
        nameLabel.Size = UDim2.new(0.5, -10, 1, 0)
        nameLabel.Position = UDim2.new(0, 10, 0, 0)
        nameLabel.Font = Enum.Font.GothamBold
        nameLabel.Text = "POSISI " .. index
        nameLabel.TextColor3 = CONFIG.COLORS.Text
        nameLabel.TextSize = 13
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left
        nameLabel.Parent = item
        
        local tpBtn = Instance.new("TextButton")
        tpBtn.BackgroundColor3 = CONFIG.COLORS.TPButton
        tpBtn.BorderSizePixel = 0
        tpBtn.Size = UDim2.new(0, 45, 0, 32)
        tpBtn.Position = UDim2.new(0.55, 0, 0.5, -16)
        tpBtn.Font = Enum.Font.GothamBold
        tpBtn.Text = "TP"
        tpBtn.TextColor3 = CONFIG.COLORS.Text
        tpBtn.TextSize = 12
        tpBtn.Parent = item
        
        local tpCorner = Instance.new("UICorner")
        tpCorner.CornerRadius = UDim.new(0, 6)
        tpCorner.Parent = tpBtn
        
        local deleteBtn = Instance.new("TextButton")
        deleteBtn.BackgroundColor3 = CONFIG.COLORS.DeleteButton
        deleteBtn.BorderSizePixel = 0
        deleteBtn.Size = UDim2.new(0, 35, 0, 32)
        deleteBtn.Position = UDim2.new(1, -45, 0.5, -16)
        deleteBtn.Font = Enum.Font.GothamBold
        deleteBtn.Text = "🗑"
        deleteBtn.TextColor3 = CONFIG.COLORS.Text
        deleteBtn.TextSize = 16
        deleteBtn.Parent = item
        
        local delCorner = Instance.new("UICorner")
        delCorner.CornerRadius = UDim.new(0, 6)
        delCorner.Parent = deleteBtn
        
        tpBtn.MouseButton1Click:Connect(function()
            CreateClickEffect(tpBtn)
            if SafeTeleport(position) then
                ShowNotification("Teleported to Checkpoint " .. index)
                Data.CurrentCheckpointIndex = index
                UpdateCurrentIndexLabel()
                
                HandleManualAutoReset()
            else
                ShowNotification("Character not ready!")
            end
        end)
        
        deleteBtn.MouseButton1Click:Connect(function()
            CreateClickEffect(deleteBtn)
            table.remove(Data.Checkpoints, index)
            item:Destroy()
            for _, child in ipairs(CheckpointsScroll:GetChildren()) do
                if child:IsA("Frame") then
                    child:Destroy()
                end
            end
            Data.CheckpointItems = {}
            for i, cp in ipairs(Data.Checkpoints) do
                CreateCheckpointItem(i, cp.Position)
            end
            UpdateCurrentIndexLabel()
            ShowNotification("Checkpoint " .. index .. " deleted!")
        end)
        
        table.insert(Data.CheckpointItems, item)
        CheckpointsScroll.CanvasSize = UDim2.new(0, 0, 0, checkpointsLayout.AbsoluteContentSize.Y + 20)
    end

    --// Refresh checkpoint list
    for i, cp in ipairs(Data.Checkpoints) do
        CreateCheckpointItem(i, cp.Position)
    end
    UpdateCurrentIndexLabel()

    local function AddCheckpoint()
        local hrp = GetHumanoidRootPart()
        if not hrp then
            ShowNotification("Character not found!")
            return
        end
        
        local pos = hrp.Position
        
        table.insert(Data.Checkpoints, {
            Position = pos,
            Name = "POSISI " .. #Data.Checkpoints + 1
        })
        
        CreateCheckpointItem(#Data.Checkpoints, pos)
        UpdateCurrentIndexLabel()
        ShowNotification("Checkpoint " .. #Data.Checkpoints .. " added!")
    end

    local function NextCheckpoint()
        if #Data.Checkpoints == 0 then
            ShowNotification("No checkpoints!")
            return
        end
        
        local nextIndex = Data.CurrentCheckpointIndex + 1
        if nextIndex > #Data.Checkpoints then
            nextIndex = 1
        end
        TeleportToCheckpoint(nextIndex)
    end

    local function PrevCheckpoint()
        if #Data.Checkpoints == 0 then
            ShowNotification("No checkpoints!")
            return
        end
        
        local prevIndex = Data.CurrentCheckpointIndex - 1
        if prevIndex < 1 then
            prevIndex = #Data.Checkpoints
        end
        TeleportToCheckpoint(prevIndex)
    end

    --// LOGIKA LOOP UTAMA (StartLoop Original)
    local function StartLoop()
        if Data.IsLooping or #Data.Checkpoints == 0 then return end
        
        Data.IsLooping = true
        Data.CurrentCheckpointIndex = 1
        
        Data.LoopConnection = task.spawn(function()
            while Data.IsLooping do
                if SafeTeleport(Data.Checkpoints[Data.CurrentCheckpointIndex].Position) then
                    
                    if Data.AutoResetEnabled and Data.CurrentCheckpointIndex == #Data.Checkpoints then
                        UpdateCurrentIndexLabel()
                        
                        task.wait(Data.ResetDelay or 0.5)
                        
                        if Data.ResetScript and Data.ResetScript ~= "" then
                            local success, result = pcall(function()
                                local func = loadstring(Data.ResetScript)
                                if func then func() end
                            end)
                            if not success then
                                warn("shhhhnayy05 - Reset Script Error: ", result)
                            end
                        end
                        
                        task.wait(Data.StartLoopDelay or 1.0)
                        Data.CurrentCheckpointIndex = 1
                    else
                        Data.CurrentCheckpointIndex = Data.CurrentCheckpointIndex + 1
                        if Data.CurrentCheckpointIndex > #Data.Checkpoints then
                            Data.CurrentCheckpointIndex = 1
                        end
                    end
                    UpdateCurrentIndexLabel()
                end
                task.wait(Data.Speed)
            end
        end)
        
        ShowNotification("Loop started!")
    end

    local function StopLoop()
        Data.IsLooping = false
        Data.LoopConnection = nil
        ShowNotification("Loop stopped!")
    end

    --// Save Config dengan tombol CLOSE
    local function SaveConfig()
        if #Data.Checkpoints == 0 then
            ShowNotification("No checkpoints to save!")
            return
        end
        
        local InputGui = Instance.new("ScreenGui")
        InputGui.Name = "shhhhnayy05_Input"
        InputGui.ResetOnSpawn = false
        InputGui.Parent = PlayerGui
        
        local overlay = Instance.new("Frame")
        overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        overlay.BackgroundTransparency = 0.5
        overlay.Size = UDim2.new(1, 0, 1, 0)
        overlay.Parent = InputGui
        
        local inputFrame = Instance.new("Frame")
        inputFrame.BackgroundColor3 = CONFIG.COLORS.Background
        inputFrame.BorderSizePixel = 0
        inputFrame.Size = UDim2.new(0, 300, 0, 180)
        inputFrame.Position = UDim2.new(0.5, -150, 0.5, -90)
        inputFrame.Parent = overlay
        
        local inputCorner = Instance.new("UICorner")
        inputCorner.CornerRadius = UDim.new(0, 12)
        inputCorner.Parent = inputFrame
        
        local inputStroke = Instance.new("UIStroke")
        inputStroke.Color = CONFIG.COLORS.Header
        inputStroke.Thickness = 2
        inputStroke.Parent = inputFrame
        
        local inputHeader = Instance.new("Frame")
        inputHeader.BackgroundColor3 = CONFIG.COLORS.Header
        inputHeader.BorderSizePixel = 0
        inputHeader.Size = UDim2.new(1, 0, 0, 40)
        inputHeader.Position = UDim2.new(0, 0, 0, 0)
        inputHeader.Parent = inputFrame
        
        local headerCorner = Instance.new("UICorner")
        headerCorner.CornerRadius = UDim.new(0, 12)
        headerCorner.Parent = inputHeader
        
        local headerCover = Instance.new("Frame")
        headerCover.BackgroundColor3 = CONFIG.COLORS.Header
        headerCover.BorderSizePixel = 0
        headerCover.Size = UDim2.new(1, 0, 0, 10)
        headerCover.Position = UDim2.new(0, 0, 1, -10)
        headerCover.Parent = inputHeader
        
        local inputTitle = Instance.new("TextLabel")
        inputTitle.BackgroundTransparency = 1
        inputTitle.Size = UDim2.new(1, -50, 1, 0)
        inputTitle.Position = UDim2.new(0, 10, 0, 0)
        inputTitle.Font = Enum.Font.GothamBold
        inputTitle.Text = "ENTER CONFIG NAME"
        inputTitle.TextColor3 = CONFIG.COLORS.Background
        inputTitle.TextSize = 16
        inputTitle.TextXAlignment = Enum.TextXAlignment.Left
        inputTitle.Parent = inputHeader
        
        local CloseInputBtn = Instance.new("TextButton")
        CloseInputBtn.Name = "CloseInputBtn"
        CloseInputBtn.BackgroundColor3 = CONFIG.COLORS.Close
        CloseInputBtn.BorderSizePixel = 0
        CloseInputBtn.Size = UDim2.new(0, 35, 0, 30)
        CloseInputBtn.Position = UDim2.new(1, -40, 0, 5)
        CloseInputBtn.Font = Enum.Font.GothamBold
        CloseInputBtn.Text = "X"
        CloseInputBtn.TextColor3 = CONFIG.COLORS.Text
        CloseInputBtn.TextSize = 16
        CloseInputBtn.Parent = inputHeader
        
        local closeInputCorner = Instance.new("UICorner")
        closeInputCorner.CornerRadius = UDim.new(0, 6)
        closeInputCorner.Parent = CloseInputBtn
        
        local textBox = Instance.new("TextBox")
        textBox.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
        textBox.BorderSizePixel = 0
        textBox.Size = UDim2.new(1, -40, 0, 40)
        textBox.Position = UDim2.new(0, 20, 0, 55)
        textBox.Font = Enum.Font.Gotham
        textBox.Text = ""
        textBox.PlaceholderText = "Config name..."
        textBox.TextColor3 = CONFIG.COLORS.Text
        textBox.TextSize = 14
        textBox.ClearTextOnFocus = false
        textBox.Parent = inputFrame
        
        local tbCorner = Instance.new("UICorner")
        tbCorner.CornerRadius = UDim.new(0, 8)
        tbCorner.Parent = textBox
        
        local saveBtn = Instance.new("TextButton")
        saveBtn.BackgroundColor3 = CONFIG.COLORS.StartLoop
        saveBtn.BorderSizePixel = 0
        saveBtn.Size = UDim2.new(0, 120, 0, 40)
        saveBtn.Position = UDim2.new(0.5, -60, 0, 110)
        saveBtn.Font = Enum.Font.GothamBold
        saveBtn.Text = "SAVE"
        saveBtn.TextColor3 = CONFIG.COLORS.Text
        saveBtn.TextSize = 14
        saveBtn.Parent = inputFrame
        
        local saveBtnCorner = Instance.new("UICorner")
        saveBtnCorner.CornerRadius = UDim.new(0, 8)
        saveBtnCorner.Parent = saveBtn
        
        local function CloseInputGUI()
            CreateTween(inputFrame, {Size = UDim2.new(0, 0, 0, 0)}, 0.2):Play()
            task.delay(0.2, function()
                InputGui:Destroy()
            end)
        end
        
        CloseInputBtn.MouseButton1Click:Connect(function()
            CreateClickEffect(CloseInputBtn)
            CloseInputGUI()
            ShowNotification("Save cancelled")
        end)
        
        saveBtn.MouseButton1Click:Connect(function()
            CreateClickEffect(saveBtn)
            local name = textBox.Text:gsub("^%s*(.-)%s*$", "%1")
            if name ~= "" then
                local checkpointsCopy = {}
                for i, cp in ipairs(Data.Checkpoints) do
                    table.insert(checkpointsCopy, {
                        Position = {
                            X = cp.Position.X,
                            Y = cp.Position.Y,
                            Z = cp.Position.Z
                        },
                        Name = cp.Name
                    })
                end
                
                Data.SavedConfigs[name] = {
                    Checkpoints = checkpointsCopy,
                    Timestamp = os.time()
                }
                
                SaveToFile()
                ShowNotification("Config '" .. name .. "' saved permanently!")
                CloseInputGUI()
                RefreshLoadList()
            else
                ShowNotification("Please enter a name!")
            end
        end)
        
        textBox.FocusLost:Connect(function(enterPressed)
            if enterPressed then
                saveBtn.MouseButton1Click:Fire()
            end
        end)
        
        inputFrame.Size = UDim2.new(0, 0, 0, 0)
        CreateTween(inputFrame, {Size = UDim2.new(0, 300, 0, 180)}, 0.3, Enum.EasingStyle.Back):Play()
    end

    local function LoadConfig(name)
        local config = Data.SavedConfigs[name]
        if config then
            Data.Checkpoints = {}
            for _, child in ipairs(CheckpointsScroll:GetChildren()) do
                if child:IsA("Frame") then
                    child:Destroy()
                end
            end
            Data.CheckpointItems = {}
            
            for _, cp in ipairs(config.Checkpoints) do
                local pos = Vector3.new(cp.Position.X, cp.Position.Y, cp.Position.Z)
                table.insert(Data.Checkpoints, {
                    Position = pos,
                    Name = cp.Name
                })
                CreateCheckpointItem(#Data.Checkpoints, pos)
            end
            
            Data.CurrentCheckpointIndex = 1
            UpdateCurrentIndexLabel()
            ShowNotification("Config '" .. name .. "' loaded!")
            LoadConfigFrame.Visible = false
        else
            ShowNotification("Config not found!")
        end
    end

    local PendingDeleteConfig = nil

    local function ShowDeleteConfirmation(configName)
        PendingDeleteConfig = configName
        DeleteConfirmGui.Visible = true
        CreateTween(ConfirmFrame, {Size = UDim2.new(0, 280, 0, 120)}, 0.2, Enum.EasingStyle.Back):Play()
    end

    local function HideDeleteConfirmation()
        PendingDeleteConfig = nil
        local closeTween = CreateTween(ConfirmFrame, {Size = UDim2.new(0, 0, 0, 0)}, 0.2)
        closeTween:Play()
        closeTween.Completed:Connect(function()
            DeleteConfirmGui.Visible = false
        end)
    end

    local function ConfirmDelete()
        if PendingDeleteConfig then
            Data.SavedConfigs[PendingDeleteConfig] = nil
            SaveToFile()
            ShowNotification("Config '" .. PendingDeleteConfig .. "' deleted!")
            RefreshLoadList()
        end
        HideDeleteConfirmation()
    end

    local function CancelDelete()
        ShowNotification("Delete cancelled")
        HideDeleteConfirmation()
    end

    local function DeleteConfig(name)
        ShowDeleteConfirmation(name)
    end

    function RefreshLoadList()
        for _, child in ipairs(ScrollFrame:GetChildren()) do
            if child:IsA("Frame") then
                child:Destroy()
            end
        end
        
        for name, data in pairs(Data.SavedConfigs) do
            local configItem = Instance.new("Frame")
            configItem.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
            configItem.BorderSizePixel = 0
            configItem.Size = UDim2.new(1, 0, 0, 50)
            configItem.Parent = ScrollFrame
            
            local itemCorner = Instance.new("UICorner")
            itemCorner.CornerRadius = UDim.new(0, 8)
            itemCorner.Parent = configItem
            
            local nameLabel = Instance.new("TextLabel")
            nameLabel.BackgroundTransparency = 1
            nameLabel.Size = UDim2.new(1, -50, 1, 0)
            nameLabel.Position = UDim2.new(0, 10, 0, 0)
            nameLabel.Font = Enum.Font.GothamBold
            nameLabel.Text = name:upper()
            nameLabel.TextColor3 = CONFIG.COLORS.Text
            nameLabel.TextSize = 14
            nameLabel.TextXAlignment = Enum.TextXAlignment.Left
            nameLabel.Parent = configItem
            
            local clickDetector = Instance.new("TextButton")
            clickDetector.BackgroundTransparency = 1
            clickDetector.Size = UDim2.new(1, -50, 1, 0)
            clickDetector.Position = UDim2.new(0, 0, 0, 0)
            clickDetector.Text = ""
            clickDetector.Parent = configItem
            
            clickDetector.MouseButton1Click:Connect(function()
                CreateClickEffect(configItem)
                LoadConfig(name)
            end)
            
            local deleteBtn = Instance.new("TextButton")
            deleteBtn.BackgroundColor3 = CONFIG.COLORS.StopLoop
            deleteBtn.BorderSizePixel = 0
            deleteBtn.Size = UDim2.new(0, 35, 0, 35)
            deleteBtn.Position = UDim2.new(1, -45, 0, 7)
            deleteBtn.Font = Enum.Font.GothamBold
            deleteBtn.Text = "🗑"
            deleteBtn.TextColor3 = CONFIG.COLORS.Text
            deleteBtn.TextSize = 18
            deleteBtn.Parent = configItem
            
            local delCorner = Instance.new("UICorner")
            delCorner.CornerRadius = UDim.new(0, 6)
            delCorner.Parent = deleteBtn
            
            deleteBtn.MouseButton1Click:Connect(function()
                CreateClickEffect(deleteBtn)
                DeleteConfig(name)
            end)
        end
        
        ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, scrollLayout.AbsoluteContentSize.Y + 20)
    end

    --// Button Connections
    CheckpointIcon.MouseButton1Click:Connect(function()
        CreateClickEffect(CheckpointIcon)
        AddCheckpoint()
    end)

    NavIcon.MouseButton1Click:Connect(function()
        CreateClickEffect(NavIcon)
        NavGui.Visible = not NavGui.Visible
        if NavGui.Visible then
            CreateTween(NavGui, {Size = CONFIG.NAV_GUI_SIZE}, 0.3, Enum.EasingStyle.Back):Play()
        end
    end)

    CloseNavBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(CloseNavBtn)
        NavGui.Visible = false
    end)

    PrevBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(PrevBtn)
        PrevCheckpoint()
    end)

    NextBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(NextBtn)
        NextCheckpoint()
    end)

    StartLoopBtn.MouseButton1Click:Connect(function()
        StartLoop()
    end)

    StopLoopBtn.MouseButton1Click:Connect(function()
        StopLoop()
        if LoopCloneBtn.Text == "STOP CLONE LOOP" then
            LoopCloneBtn.Text = "LOOP CLONE"
            LoopCloneBtn.BackgroundColor3 = CONFIG.COLORS.AddCheckpoint
        end
    end)

    SaveConfigBtn.MouseButton1Click:Connect(function()
        SaveConfig()
    end)

    LoadConfigBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(LoadConfigBtn)
        LoadConfigFrame.Visible = not LoadConfigFrame.Visible
        if LoadConfigFrame.Visible then
            RefreshLoadList()
        end
    end)

    CloseLoadBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(CloseLoadBtn)
        LoadConfigFrame.Visible = false
    end)

    MinusBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(MinusBtn)
        Data.Speed = math.max(0.1, Data.Speed - 0.1)
        Data.Speed = math.floor(Data.Speed * 10) / 10
        SpeedLabel.Text = "SPEED: " .. Data.Speed .. "s"
    end)

    PlusBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(PlusBtn)
        Data.Speed = math.min(10, Data.Speed + 0.1)
        Data.Speed = math.floor(Data.Speed * 10) / 10
        SpeedLabel.Text = "SPEED: " .. Data.Speed .. "s"
    end)

    SettingsBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(SettingsBtn)
        SettingsWindow.Visible = not SettingsWindow.Visible
    end)

    CloseSettingsBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(CloseSettingsBtn)
        SettingsWindow.Visible = false
    end)

    YesBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(YesBtn)
        ConfirmDelete()
    end)

    NoBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(NoBtn)
        CancelDelete()
    end)

    --// LOGIKA CLONE TP SYSTEM
    CloneTPBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(CloneTPBtn)
        CloneWindow.Visible = not CloneWindow.Visible
    end)

    CloseCloneBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(CloseCloneBtn)
        CloneWindow.Visible = false
    end)

    AddCloneBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(AddCloneBtn)
        
        if Data.CloneModel then
            -- Remove Clone System
            Data.CloneModel:Destroy()
            Data.CloneModel = nil
            
            local char = LocalPlayer.Character
            if char then
                local cam = workspace.CurrentCamera
                local hum = char:FindFirstChild("Humanoid")
                if hum then
                    cam.CameraSubject = hum
                end
            end
            
            AddCloneBtn.Text = "ADD CLONE"
            AddCloneBtn.BackgroundColor3 = CONFIG.COLORS.StartLoop
            ShowNotification("Clone Removed! Kamera kembali normal.")
        else
            -- Add Clone System
            local char = LocalPlayer.Character
            if not char then return end
            
            char.Archivable = true
            local clone = char:Clone()
            clone.Name = "shhhhnayy05_Dummy"
            
            for _, v in ipairs(clone:GetDescendants()) do
                if v:IsA("Script") or v:IsA("LocalScript") then
                    v.Enabled = false
                elseif v:IsA("BasePart") then
                    v.Anchored = true
                    v.CanCollide = false
                end
            end
            
            clone.Parent = workspace
            clone:PivotTo(char:GetPivot())
            
            local cam = workspace.CurrentCamera
            local hum = clone:FindFirstChild("Humanoid")
            if hum then
                cam.CameraSubject = hum
            end
            
            Data.CloneModel = clone
            
            AddCloneBtn.Text = "REMOVE CLONE"
            AddCloneBtn.BackgroundColor3 = CONFIG.COLORS.StopLoop
            ShowNotification("Clone Added! Karakter tertahan, kamera terkunci pada Clone.")
        end
    end)

    LoopCloneBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(LoopCloneBtn)
        
        if not Data.CloneModel then
            ShowNotification("Silahkan Add Clone terlebih dahulu!")
            return
        end
        
        if Data.IsLooping then
            StopLoop()
            LoopCloneBtn.Text = "LOOP CLONE"
            LoopCloneBtn.BackgroundColor3 = CONFIG.COLORS.AddCheckpoint
        else
            if #Data.Checkpoints == 0 then
                ShowNotification("No checkpoints to loop!")
                return
            end
            StartLoop()
            LoopCloneBtn.Text = "STOP CLONE LOOP"
            LoopCloneBtn.BackgroundColor3 = CONFIG.COLORS.StopLoop
        end
    end)

    --// Minimize System
    local function MinimizeGUI()
        Data.IsMinimized = true
        local closeTween = CreateTween(MainFrame, {Size = UDim2.new(0, 0, 0, 0)}, 0.3)
        closeTween:Play()
        closeTween.Completed:Connect(function()
            MainFrame.Visible = false
            MinimizeIcon.Visible = true
            CreateTween(MinimizeIcon, {Size = UDim2.new(0, 55, 0, 55)}, 0.2, Enum.EasingStyle.Back):Play()
            task.delay(0.2, function()
                CreateTween(MinimizeIcon, {Size = UDim2.new(0, CONFIG.ICON_SIZE, 0, CONFIG.ICON_SIZE)}, 0.2):Play()
            end)
        end)
    end

    local function RestoreGUI()
        Data.IsMinimized = false
        MainFrame.Visible = true
        MinimizeIcon.Visible = false
        MainFrame.Size = UDim2.new(0, 0, 0, 0)
        MainFrame.Position = UDim2.new(0.5, -200, 0.5, -200)
        CreateTween(MainFrame, {Size = CONFIG.GUI_SIZE}, 0.4, Enum.EasingStyle.Back):Play()
    end

    MinimizeBtn.MouseButton1Click:Connect(function()
        CreateClickEffect(MinimizeBtn)
        MinimizeGUI()
    end)

    MinimizeIcon.MouseButton1Click:Connect(function()
        CreateClickEffect(MinimizeIcon)
        RestoreGUI()
    end)

    --// Dragging System
    local function MakeDraggable(frame, handle)
        local dragging = false
        local dragInput, dragStart, startPos
        
        handle.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or 
               input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
                dragStart = input.Position
                startPos = frame.Position
                
                input.Changed:Connect(function()
                     if input.UserInputState == Enum.UserInputState.End then
                        dragging = false
                    end
                end)
            end
        end)
        
        handle.InputChanged:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseMovement or 
               input.UserInputType == Enum.UserInputType.Touch then
                dragInput = input
            end
        end)
        
        UserInputService.InputChanged:Connect(function(input)
            if input == dragInput and dragging then
                local delta = input.Position - dragStart
                frame.Position = UDim2.new(
                    startPos.X.Scale, startPos.X.Offset + delta.X,
                    startPos.Y.Scale, startPos.Y.Offset + delta.Y
                )
            end
        end)
    end

    MakeDraggable(MainFrame, Header)
    MakeDraggable(LoadConfigFrame, LoadHeader)
    MakeDraggable(SettingsWindow, SettingsHeader)
    MakeDraggable(CloneWindow, CloneHeader)
    MakeDraggable(NavGui, NavHeader)

    --// Mobile Support
    if UserInputService.TouchEnabled then
        CheckpointIcon.Size = UDim2.new(0, 60, 0, 60)
        NavIcon.Size = UDim2.new(0, 60, 0, 60)
        MinimizeIcon.Size = UDim2.new(0, 60, 0, 60)
        
        CheckpointIcon.Position = UDim2.new(1, -70, 0, 10)
        NavIcon.Position = UDim2.new(1, -70, 0, 80)
        MinimizeIcon.Position = UDim2.new(0, 10, 0, 10)
        
        NavGui.Position = UDim2.new(1, -180, 0, 80)
        LoadConfigFrame.Position = UDim2.new(0.5, -150, 0.5, -150)
        SettingsWindow.Position = UDim2.new(0.5, -160, 0.5, -155)
        CloneWindow.Position = UDim2.new(0.5, -150, 0.5, -90)
    end

    --// Animation awal jika minimize
    if Data.IsMinimized then
        CreateTween(MinimizeIcon, {Size = UDim2.new(0, 60, 0, 60)}, 0.3, Enum.EasingStyle.Back):Play()
        task.delay(0.3, function()
            CreateTween(MinimizeIcon, {Size = UDim2.new(0, CONFIG.ICON_SIZE, 0, CONFIG.ICON_SIZE)}, 0.2):Play()
        end)
    else
        MainFrame.Size = UDim2.new(0, 0, 0, 0)
        CreateTween(MainFrame, {Size = CONFIG.GUI_SIZE}, 0.4, Enum.EasingStyle.Back):Play()
    end

    Data.GUIInitialized = true
    print("shhhhnayy05 - GUI Created/Restored")
end

--// Character Respawn Handler - PENTING!
local function OnCharacterAdded(character)
    if Data.CloneModel then
        Data.CloneModel:Destroy()
        Data.CloneModel = nil
        if Data.IsLooping then
            Data.IsLooping = false
            Data.LoopConnection = nil
        end
    end
    
    local hrp = character:WaitForChild("HumanoidRootPart", 5)
    if hrp then
        ShowNotification("Character respawned! GUI tetap aktif.")
    end
end

--// Setup Respawn Handler
LocalPlayer.CharacterAdded:Connect(function(newCharacter)
    OnCharacterAdded(newCharacter)
end)

--// Jalankan GUI pertama kali
CreateGUI()
ShowNotification("shhhhnayy05 - Auto Teleport Loaded! File aman.")

print("shhhhnayy05 - Auto Teleport GUI Loaded Successfully!")
print("File simpanan: " .. CONFIG.SAVE_FILE)
print("Data checkpoints: " .. tostring(#Data.Checkpoints) .. " items")
